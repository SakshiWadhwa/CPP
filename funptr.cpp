#include<stdio.h>
#include<stdlib.h>

int add( int x, int y)
{
	return (x + y);
}

int total(int x ())
{
 	int y = x(10, 2);
 	return y;
}

int main( void )
{
	int res;
	res = total (add) ;
	printf("%d\n", res);
	return 0;
}
